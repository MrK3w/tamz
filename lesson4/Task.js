class Task {
    constructor(text, date,important,notes,colors,favorite,days) {
      this.text = text;
      this.date = date;
      this.important = important;
      this.notes = notes;
      this.colors = colors;
      this.favorite = favorite;
      this.days = days;
    }
  }